using UnityEngine;
using System.Collections;

public class FoodInitTutorial : MonoBehaviour {

	public GameObject text;
	public GameObject trigger;

	// Use this for initialization
	void Start () {
		text.SetActive (true);
		trigger.SetActive (true);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
